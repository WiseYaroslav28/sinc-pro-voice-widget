# Журнал решений проекта (Architecture Decision Records)

> Автоматически сгенерировано Memory Engine. Последнее обновление: 2026-07-12T04:59:59.422Z

---

## Актуальные решения

### [ADR-0017] 3-позиционная раскладка и автопереключение языка ввода Windows по хоткею
* **Дата:** 2026-07-12
* **Статус:** `Active`
* **Решение:** Реализован 3-позиционный переключатель раскладки (0 - Выкл, 1 - Ручной хоткей, 2 - Автозамена). Добавлено автопереключение системного языка ввода в Windows (через WM_INPUTLANGCHANGEREQUEST активному окну) при срабатывании хоткея Ctrl + Pause на основе анализа направления перевода текста, позволяя пользователю продолжать печатать без переключения языка вручную.

### [ADR-0016] Релокация кнопки Play, сворачивание виджета по hover и локальный fallback ИИ
* **Дата:** 2026-07-12
* **Статус:** `Active`
* **Решение:** Реализован перенос кнопки Play в начало виджета озвучки. Сделано раскрытие виджета вправо. Сворачивание и разворачивание привязано к элементам. Исправлен баг обрезки комбобоксов: overflow-hidden удален из tts-extra-content в tts-widget.js и перенесен в .hover-collapsed в widget.html, что позволяет выпадающим меню отображаться в развернутом виде. Добавлено автоматическое сворачивание второго ряда и сброс иконки кнопки expand при уходе курсора с виджета в widget.js. Исправлено дергание кнопок при анимации, устранена обрезка тени справа и рывки при сворачивании виджета. Встроена цепочка локальных fallback-моделей ASR (Whisper) и LLM (локальные серверы/Ollama) при сбоях API Gemini.

### [ADR-0015] Релокация кнопки Play, сворачивание виджета по hover и локальный fallback ИИ
* **Дата:** 2026-07-12
* **Статус:** `Active`
* **Решение:** Реализован перенос кнопки Play в начало виджета озвучки, раскрытие вправо и селективное наведение. Добавлен круговой SVG-прогресс-бар вокруг кнопки Play, который отображает процент буферизации (полупрозрачный бирюзовый) и процент воспроизведения (бирюзовый) на основе событий прогресса из VoiceCore, транслируемых по Tauri. Исправлено дергание кнопок при анимации, устранена обрезка тени справа и рывки при сворачивании виджета. Встроена цепочка локальных fallback-моделей ASR (Whisper) и LLM (локальные серверы/Ollama) при сбоях API Gemini.

### [ADR-0014] Релокация кнопки Play, сворачивание виджета по hover и локальный fallback ИИ
* **Дата:** 2026-07-12
* **Статус:** `Active`
* **Решение:** Реализован перенос кнопки Play в начало виджета озвучки. Сделано раскрытие виджета вправо. Сворачивание и разворачивание привязано к элементам. Исправлено дергание кнопок при анимации: класс justify-center у tts-widget-root изменен на justify-start, фиксируя кнопки слева. Устранена обрезка тени справа: ширина окна Tauri увеличена до 580px, а хитбокс до 576px. Устранены рывки при сворачивании: в JS добавлен setTimeout на 350мс, откладывающий применение узкого WinAPI хитбокса до полного завершения сжатия виджета. Встроена цепочка локальных fallback-моделей ASR (Whisper) и LLM (локальные серверы/Ollama) при сбоях API Gemini.

### [ADR-0013] Релокация кнопки Play, сворачивание виджета по hover и локальный fallback ИИ
* **Дата:** 2026-07-12
* **Статус:** `Active`
* **Решение:** Реализован перенос кнопки Play в начало виджета озвучки. Сделано раскрытие виджета вправо. Сворачивание и разворачивание привязано селективно к элементам. Исправлен баг вертикальной обрезки рамки и теней в свернутом состоянии: ширина .hover-collapsed увеличена до 114px, ширина хитбокса hover_collapsed увеличена до 210px (чтобы тени и свечение не срезались регионом WinAPI), а также исправлен обработчик widget-shown для корректного переприменения хитбокса hover_collapsed при запуске и показе окна. Встроена цепочка локальных fallback-моделей ASR (Whisper) и LLM (локальные серверы/Ollama) при сбоях API Gemini.

### [ADR-0012] Релокация кнопки Play, сворачивание виджета по hover и локальный fallback ИИ
* **Дата:** 2026-07-12
* **Статус:** `Active`
* **Решение:** Реализован перенос кнопки Play в начало виджета озвучки. Сделано раскрытие виджета вправо (через justify-content: flex-start и padding-left: 30px в widget.html). Сворачивание и разворачивание привязано селективно к элементам. Исправлен баг вертикальной обрезки рамки в свернутом состоянии: ширина .hover-collapsed увеличена до 114px, а overflow: hidden убран, так как контент теперь полностью помещается внутри. Также встроена цепочка локальных fallback-моделей ASR (Whisper) и LLM (локальные серверы/Ollama) при сбоях API Gemini.

### [ADR-0011] Релокация кнопки Play, сворачивание виджета по hover и локальный fallback ИИ
* **Дата:** 2026-07-12
* **Статус:** `Active`
* **Решение:** Реализован перенос кнопки Play в начало виджета озвучки. Сделано раскрытие виджета вправо (через justify-content: flex-start и padding-left: 30px в widget.html). Сворачивание и разворачивание привязано селективно к элементам: наведение на кнопку Play или элементы правее раскрывает виджет, наведение на Drag Handle или уход мыши с виджета сворачивает его. Также встроена цепочка локальных fallback-моделей ASR (Whisper) и LLM (локальные серверы/Ollama) при сбоях API Gemini.

### [ADR-0010] Релокация кнопки Play, сворачивание виджета по hover и локальный fallback ИИ
* **Дата:** 2026-07-12
* **Статус:** `Active`
* **Решение:** Реализован перенос кнопки Play в начало виджета озвучки, автосворачивание плавающего виджета по hover до 104px (с изменением хитбокса set_click_region Tauri), а также автоматическая цепочка fallback на локальный Whisper (ASR) и Ollama/локальные серверы из настроек (LLM) при сбоях Gemini API.

### [ADR-0009] [Auto] Да, всё заработало. Давай теперь выложим это дело на GitHub, задокументируем доп
* **Дата:** 2026-07-11
* **Статус:** `Active`
* **Категория:** UX
* **Действие:** APPROVED
* **Решение:** Да, всё заработало. Давай теперь выложим это дело на GitHub, задокументируем дополнительно, что мы сделали, примем эту работу, доработку, как выполненную и внесём необходимые изменения в документацию,
* **Источник:** диалог `02c3f38e-cd3f-4b03-984f-1bdad899603e`, turn 0

### [ADR-0008] [Auto] Странно, с клавишей Control не очень работает. Когда я сменил Shift пауза, она р
* **Дата:** 2026-07-11
* **Статус:** `Active`
* **Категория:** UX
* **Действие:** CHANGED
* **Решение:** Странно, с клавишей Control не очень работает. Когда я сменил Shift пауза, она работает, а если делаю Control пауза, то прекращает работать. С чем это может быть связано? Может быть у нас на Control е
* **Источник:** диалог `02c3f38e-cd3f-4b03-984f-1bdad899603e`, turn 2

### [ADR-0007] [Auto] Хочу добавить функцию, которая бы при нажатии клавиши Ctrl пауза меняла язык выд
* **Дата:** 2026-07-11
* **Статус:** `Active`
* **Категория:** UX
* **Действие:** APPROVED
* **Решение:** Хочу добавить функцию, которая бы при нажатии клавиши Ctrl пауза меняла язык выделенный текст. То есть символы с русской раскладки на английскую и наоборот.И давай также тумблер переключения, который 
* **Источник:** диалог `02c3f38e-cd3f-4b03-984f-1bdad899603e`, turn 0

### [ADR-0006] Безопасная инициализация и частичное обновление конфигурации (TTS скорость & магнит)
* **Дата:** 2026-06-25
* **Статус:** `Active`
* **Категория:** Architecture
* **Действие:** APPROVED
* **Контекст:** При перезапуске приложения или изменении языка/модели в главном меню сбрасывались скорость озвучки (TTS) и магнит капсулы. Это происходило из-за: 1) полной перезаписи конфига неполным объектом из index.html, 2) гонки условий при асинхронном чтении конфига при старте тяжелого главного окна (setTimeout на 500мс срабатывал раньше чтения и рассылал дефолтные 1.0 в плавающий виджет).
* **Решение:** 1. Заменили вызов save_config на update_config_fields в главном меню фронтенда для предотвращения перезаписи TTS настроек дефолтными значениями при сохранении других параметров. 2. Устранили гонку условий при старте окон: удалили ненадежный setTimeout на 500мс из voice-core.js и перенесли широковещательную рассылку broadcastState('settings') внутрь обработчика успешной загрузки load_config. Настройки теперь рассылаются строго по факту чтения с диска.
* **Связанные файлы:** src/index.html, src/voice-core.js

### [ADR-0005] Единый источник настроек окон и автозапуска через config.json на бэкенде
* **Дата:** 2026-06-24
* **Статус:** `Active`
* **Категория:** Architecture
* **Решение:** Все настройки окон (режимы работы, скорость/голос TTS, магнит, автоперевод) перенесены на бэкенд в config.json в качестве Single Source of Truth. Окна асинхронно опрашивают бэкенд при загрузке. При старте бэкенд автоматически обновляет путь автозапуска в реестре Windows.

### [ADR-0004] Синхронизация сохраненной скорости TTS с UI при запуске
* **Дата:** 2026-06-24
* **Статус:** `Active`
* **Категория:** UX
* **Сущность:** Виджет TTS (tts-widget.js)
* **Действие:** APPROVED
* **Контекст:** Исправление неверного отображения скорости (1.0x вместо сохраненного значения) на кнопке скорости воспроизведения при запуске приложения.
* **Решение:** При инициализации виджета tts-widget.js добавлен вызов функции updateSpeedUI(currentSpeed) сразу после ее объявления. Это гарантирует, что при первом запуске приложения скорость на кнопке панели управления и слайдер тонкой настройки синхронизируются с реальным сохраненным значением из localStorage.
* **Связанные файлы:** src/tts-widget.js

### [ADR-0003] Автоматический перезапуск микрофона после сна ОС
* **Дата:** 2026-06-24
* **Статус:** `Active`
* **Категория:** Architecture
* **Сущность:** Микрофон (capsule.html)
* **Действие:** APPROVED
* **Контекст:** Решение проблемы зависания диктовки/записи после долгого простоя или глубокого сна операционной системы.
* **Решение:** Перед каждым стартом записи в startRecordingProcess выполняется валидация закешированного MediaStream (проверка active и live-статуса треков). Если поток протух (например, после сна ОС), старые треки освобождаются через track.stop(), и поток инициализируется заново через getUserMedia.
* **Связанные файлы:** src/capsule.html

### [ADR-0002] Предотвращение принудительного показа капсулы в фоновом режиме
* **Дата:** 2026-06-24
* **Статус:** `Active`
* **Категория:** UX
* **Сущность:** Капсула (capsule.html)
* **Действие:** APPROVED
* **Контекст:** Исправление бага, при котором скрытая капсула выводилась на экран при скрытии главного дашборда или при переходе курсора на другой монитор.
* **Решение:** В функцию resizeToState добавлена проверка режима капсулы. Если режим равен 1 (скрыта, но работает) и состояние неактивно (idle/hover), вызовы позиционирования через Rust SetWindowPos блокируются, что предотвращает принудительное отображение окна на экране.
* **Связанные файлы:** src/capsule.html

### [ADR-0001] Паспорт проекта: SINC PRO Voice Widget
* **Дата:** 2026-06-24
* **Статус:** `Active`
* **Категория:** Architecture
* **Сущность:** Проект: SINC PRO
* **Действие:** APPROVED
* **Контекст:** Инициализация паспорта проекта при подключении Memory Engine.
* **Решение:** Разработка голосового ассистента с функциями умной диктовки (Капсула), озвучивания текста (Виджет TTS) и перевода экрана (OCR) на базе Tauri 2.0 (Rust + HTML/JS).
* **Связанные файлы:** package.json, src-tauri/Cargo.toml
